'use client';

export default function TopBar() {
    return (
        <div className="h-14 bg-[#212529] border-b border-black/10 flex items-center justify-between px-4 sticky top-0 z-40">
            {/* Top header emptied as per user request */}
        </div>
    );
}
